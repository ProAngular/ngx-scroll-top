name: Verify Pull Request
"on": pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
      - name: Build Check 🔧
        run: |
          npm ci
          npm run lint
          npm run prettier:check
          npm run build:prod
